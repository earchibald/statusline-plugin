---
description: Configure the Claude Code statusline (segments, colors, ordering) by editing ~/.claude/statusline-plugin/config.json
argument-hint: [free-text request, e.g. "show model + git branch + cost"]
---

The user wants to configure their Claude Code statusline.

Invoke the **statusline-config** skill to handle this request. The skill carries the full schema reference, default config, install steps for `~/.claude/settings.json`, and edit recipes.

User request:

$ARGUMENTS
